# Server-only. The FastAPI base URL the Next server proxies to.
# Never exposed to the browser bundle.
HEALTHX_API_URL=http://localhost:8000

# Cookie security. Set to "false" only for local http development.
HEALTHX_COOKIE_SECURE=false
